#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/* Enter 2 digits. */

int main(int argc, char *argv[]) {
	int i, n, last = 5;
	srand(time (0));
	for(i = 0; i <= last, i++;)
	{
		printf("%d\n",rand());
	}
	return 0;
}
